FROM rust:latest

# System deps for namespace/cgroup/mount operations + egress veth setup
RUN apt-get update && apt-get install -y \
    procps \
    cgroup-tools \
    util-linux \
    iproute2 \
    iptables \
    netcat-openbsd \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace
